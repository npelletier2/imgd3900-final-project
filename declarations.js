/**
 * Object properties:
 * controllable: controlled with controls if truthy
 * setup: setup function, called on game start
 * update: called every frame
 */
let objects = {};

let map;
let controls;
let camera;