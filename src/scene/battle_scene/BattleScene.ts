import "phaser"
import { BaseScene } from "../BaseScene"

export class BattleScene extends BaseScene{
    constructor(){
        super('BaseScene')
    }

    preload(): void {
        super.preload();
    }

    create(): void {
        super.create();
    }

    update(): void {
        super.update();
    }
}